{
  "name": "cortex-brain",
  "version": "1.0.0",
  "description": "Cortex brain manager — scaffold, manage, analyze, and evolve Cortex projects",
  "preferGlobal": true,
  "bin": {
    "cortex": "dist/index.js"
  },
  "files": [
    "dist",
    "src/template"
  ],
  "scripts": {
    "build": "node esbuild.config.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "esbuild": "^0.20.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.6.0"
  }
}
