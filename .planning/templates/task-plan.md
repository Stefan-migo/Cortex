# Task Plan: {name}

<task>
  <name>{Task name}</name>
  <description>{Brief description}</description>
  <files>
    {file paths to modify}
  </files>
  <action>
    {Specific implementation instructions}
    {Include edge cases, error handling, style preferences}
  </action>
  <verify>
    {How to verify correctness — e.g., curl command, test to run}
  </verify>
  <done>
    {Completion criteria — when is this truly done?}
  </done>
</task>
